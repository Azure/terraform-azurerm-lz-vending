# Idempotent Role Assignments

This example shows how to create idempotent role assignmetns using this module.

Note use of `resource_group_scope_key` to ensure that dependencies are created before role assignments are made.
